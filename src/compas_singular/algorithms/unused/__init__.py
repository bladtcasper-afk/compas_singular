"""Features of Oval's thesis that no current workflow or script uses:
interpolation layout, mesh/strip-graph isomorphism, mesh mapping and
two-colourable projection.

Not imported by ``compas_singular.algorithms``; import a module explicitly,
e.g. ``from compas_singular.algorithms.unused.twocoloring import TwoColourableProjection``.
"""
